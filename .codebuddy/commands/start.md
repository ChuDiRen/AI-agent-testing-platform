---
name: start
description: 项目启动命令。调用 project-bootstrapper 执行完整的项目启动流程。
---

# 项目启动命令

## 使用方式

```
/start [项目名称或描述]
/start @/path/to/需求文档.md        # 基于需求文档启动
```

## 功能说明

调用 project-bootstrapper Agent，支持三种敏捷模式：

**🆕 新项目模式：** 从零开始，完整 6 阶段流程（仅1次确认）
```
阶段1: 项目初始化 → README.md, 目录结构
阶段2: 需求分析   → docs/PRD.md
阶段3: 数据库设计 → docs/database-design.md, sql/schema.sql
阶段4: 原型设计   → prototype/*.html（必须生成）
阶段5: 任务拆分   → docs/frontend-tasks.md, docs/backend-tasks.md
阶段6: 开发准备   → 环境配置, 依赖安装
```

**🔄 迭代模式：** 在现有项目上增量添加新功能（敏捷开发）
```
阶段1: 需求更新 → PRD.md 增量添加功能
阶段2: 数据库设计 → 更新 schema.sql（如涉及新表）
阶段3: 原型设计 → 更新 prototype/（如涉及新页面）
阶段4: 任务拆分   → 更新相关任务清单
阶段5: API文档更新 → 更新 API 文档

支持参数控制（可组合使用）：
- --prd-only      # 只更新 PRD
- --db-only       # 只更新数据库设计
- --ui-only       # 只更新原型
- --tasks-only    # 只更新任务清单
- --api-only      # 只更新 API 文档
```

**🛠️ 开发执行模式：** 按任务清单开发已定义的功能
```
不生成新文档，只读取任务清单并分派给对应的开发 Agent
@backend-developer 执行后端任务
@frontend-developer 执行前端任务
```

## ⚠️ 文件集中管理规则

**关键：当用户指定了具体项目目录或需求文档路径时，所有生成的文档必须在该目录下！**

例如：
```
/start @/d:/project/backend/需求.md
```
→ 项目目录 = `d:/project/backend/`
→ 所有 docs/、sql/、prototype/ 都在 `d:/project/backend/` 下

## 示例

### 基于需求文档启动
```
/start @/d:/AI-agent-testing-platform/cyberpunk-backend/接口自动化智能体平台需求文档.md
```

输出结构：
```
cyberpunk-backend/              # 需求文档所在目录
├── docs/                       # 文档集中在项目目录内
│   ├── PRD.md
│   ├── architecture.md
│   ├── database-design.md
│   ├── func.md
│   ├── frontend-tasks.md
│   ├── backend-tasks.md
│   └── api/
├── sql/                        # SQL脚本在项目目录内
│   ├── schema.sql
│   └── init-data.sql
└── prototype/                  # 原型页面在项目目录内（必须生成）
    ├── index.html
    ├── dashboard.html
    ├── css/styles.css
    └── js/main.js
```

### 新建项目（新项目模式）

```
/start 订单管理系统

输出结构：
{项目目录}/
├── README.md
├── docs/
│   ├── {timestamp}/           # 📁 时间戳目录（如：20260115-143000）
│   │   ├── PRD.md
│   │   ├── architecture.md
│   │   ├── database-design.md
│   │   ├── frontend-tasks.md
│   │   ├── backend-tasks.md
│   │   ├── func.md
│   │   ├── api/
│   │   ├── sql/
│   │   └── prototype/
│   └── archive/           # 归档目录
└── ...
```

### 敏捷迭代（迭代模式）

```
/start 添加用户管理后端API --prd-only

输出：
{项目目录}/docs/{timestamp}/
├── PRD.md           ✅ 更新（新增用户管理模块）
├── database-design.md  ✅ 更新（如果涉及新表）
├── api/              ✅ 更新（新增API文档）
└── ...
```

**敏捷迭代示例：**
```bash
# 添加新功能到 PRD
/start 添加用户管理模块 --prd-only

# 只更新数据库设计
/start 优化数据表结构 --db-only

# 只更新原型
/start 更新登录页面设计 --ui-only

# 组合使用
/start 添加新功能 --prd-only --db-only
```

## 模式选择

project-bootstrapper 会自动检测项目状态：

| 状态 | 模式 | 说明 |
|------|------|------|
| docs/PRD.md 不存在 | 🆕 新项目模式 | 完整 6 阶段流程 |
| PRD 存在，功能未定义 | 🔄 迭代模式 | 增量添加功能 |
| PRD 存在，功能已定义 | 🛠️ 开发执行模式 | 按任务清单开发 |

## 快捷选项

```
/start 订单管理系统 --type=admin        # 管理后台
/start 订单管理系统 --tech=vue+fastapi  # 指定技术栈
/start 订单管理系统 --dir=my-project    # 指定项目目录
```

## 唯一确认点

**新项目模式：**
仅在 **技术栈与功能清单** 环节需要用户确认：
- 输入"确认"后自动执行阶段2-6（不再分段停顿/逐段确认）

**迭代模式和开发执行模式：**
无需确认，直接执行相应的阶段或开发任务

## 输出物

**新项目模式完成后项目包含：**
```
{项目目录}/
├── README.md
├── docs/
│   ├── {timestamp}/           # 📁 时间戳目录（如：20260115-143000）
│   │   ├── PRD.md
│   │   ├── architecture.md
│   │   ├── database-design.md
│   │   ├── frontend-tasks.md
│   │   ├── backend-tasks.md
│   │   ├── func.md
│   │   ├── api/
│   │   │   └── *.md
│   │   ├── sql/
│   │   │   ├── schema.sql
│   │   │   └── init-data.sql
│   │   └── prototype/
│   │       ├── index.html
│   │       ├── *.html
│   │       ├── css/styles.css
│   │       └── js/main.js
│   └── archive/           # 归档目录（存放历史版本）
```

**迭代模式完成后输出：**
只更新被修改的部分，例如：
- PRD.md 新增功能
- database-design.md 新增数据表
- prototype/ 新增页面
- api/ 新增接口文档

## ❌ 禁止行为

- ❌ 在工作区根目录创建分散的 docs/、sql/ 目录
- ❌ 跳过原型设计阶段（必须生成 prototype/）
- ❌ 文档分散在多个位置
