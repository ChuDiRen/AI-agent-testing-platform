[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning

# 标记定义
markers =
    api: API 接口测试
    e2e: E2E 端到端测试
    slow: 慢速测试
    smoke: 冒烟测试
