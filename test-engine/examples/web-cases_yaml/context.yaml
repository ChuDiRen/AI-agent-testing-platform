_browser:
  grid_url: ""
  driver_path: ""
  auto_download: true  # 是否自动下载浏览器驱动，默认为true
  capability:
    browserName: chrome
    # 谷歌浏览器专用配置（按需配置）
    # goog:chromeOptions: 
      # args:
      # 设置是否为 无头 隐藏模式（按需配置）
      # - "--headless"
      # 设置浏览器数据保存位置（按需配置）
      # - "--user-data-dir=d:\\tttt"
    # 火狐浏览器专用配置（按需配置）
    # moz:firefoxOptions: 
      # args:
      # 设置是否为 无头 隐藏模式（按需配置）
      # - "--headless"
    

_database:
  mysql001:
    host: 192.168.1.111
    port: 3306
    user: root
    password: root
    db: novel_plus
  mysql002:
    host: 
    port: 
    user: 
    password: 
    db: 
  redis:
    host: 
    port: 
    password: 
    db: 

