# Commit Commands

## Overview
Git workflow automation for committing, pushing, and creating pull requests.

## Components
- **Commands**:
  - `/commit`: Streamlined commit
  - `/commit-push-pr`: Commit, push, and create PR
  - `/clean_gone`: Clean up merged branches

## Usage
Use the slash commands to automate your git workflow.
