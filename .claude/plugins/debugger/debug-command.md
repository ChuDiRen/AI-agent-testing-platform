# /debug - 问题排查

## 描述
系统化排查代码问题，快速定位根因。

## 使用方式
```
/debug <问题描述> [--scope <范围>] [--agent]
```

## 参数
| 参数 | 说明 | 默认值 |
|------|------|--------|
| `问题描述` | 错误信息或问题描述 | 必填 |
| `--scope` | `frontend`/`backend`/`all` | `all` |
| `--agent` | 使用 debugger Agent | 否 |

## 排查流程

参考 `@templates/debug-patterns.md`：

```
1️⃣ 复现问题 → 获取错误信息、确认复现步骤
2️⃣ 定位范围 → 前端/后端？哪个模块？
3️⃣ 分析原因 → 查看日志、断点调试
4️⃣ 验证修复 → 修复代码、回归测试
```

## 常见错误速查

参考 `@templates/debug-patterns.md` 中的错误速查表。

## 输出格式

```
╔══════════════════════════════════════════════════════════════════╗
║                    🔍 问题排查报告                                ║
╠══════════════════════════════════════════════════════════════════╣
║  📋 问题描述                                                      ║
║  用户登录接口返回 500 错误                                        ║
╠══════════════════════════════════════════════════════════════════╣
║  🔎 排查过程                                                      ║
║  1. 查看错误日志：IntegrityError at authService.py:45            ║
║  2. 定位代码：用户表主键冲突                                      ║
╠══════════════════════════════════════════════════════════════════╣
║  ✅ 根因分析                                                      ║
║  用户注册时手动生成 ID，高并发下产生重复                          ║
╠══════════════════════════════════════════════════════════════════╣
║  💡 修复方案                                                      ║
║  1. 改用数据库自增 ID                                             ║
╚══════════════════════════════════════════════════════════════════╝
```

## 示例
```bash
/debug "登录接口 500 错误"
/debug "页面加载缓慢" --scope frontend
/debug "数据库连接超时" --agent
```
