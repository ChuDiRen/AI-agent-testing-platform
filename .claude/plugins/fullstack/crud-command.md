# /crud - CRUD 生成

## 描述
快速生成标准 CRUD 代码，包含前后端完整实现。

## 使用方式
```
/crud <模块名> [--fields <字段定义>] [--scope <范围>]
```

## 参数
| 参数 | 说明 | 默认值 |
|------|------|--------|
| `模块名` | 模块名称（如 User） | 必填 |
| `--fields` | 字段定义 | 默认字段 |
| `--scope` | `frontend`/`backend`/`fullstack` | `fullstack` |

## 字段定义格式
```
name:type:description
```

类型支持：`str`, `int`, `float`, `bool`, `datetime`, `text`

## 生成内容

参考 `@templates/code-patterns.md`：

### 后端文件
```
apitest/
├── model/{module}.py           # SQLModel 模型
├── schemas/{Module}Schema.py   # Pydantic Schema
├── service/{module}Service.py  # 业务逻辑
└── api/{Module}Controller.py   # API 接口
```

### 前端文件
```
views/{module}/
├── {Module}List.vue    # 列表页
├── {Module}Form.vue    # 表单弹窗
└── {module}.js         # API 接口
```

## 示例

```bash
# 基础用法
/crud User

# 指定字段
/crud Product --fields "name:str:产品名称,price:float:价格,stock:int:库存"

# 仅后端
/crud Order --scope backend

# 仅前端
/crud Category --scope frontend
```

## 输出格式

```
╔══════════════════════════════════════════════════════════════════╗
║                    ✅ CRUD 生成完成                               ║
╠══════════════════════════════════════════════════════════════════╣
║  📋 模块：Product                                                 ║
║  📊 字段：name(str), price(float), stock(int)                    ║
╠══════════════════════════════════════════════════════════════════╣
║  📁 生成文件                                                      ║
║  ─────────────────────────────────────────────────────────────   ║
║  后端 (4 个文件)                                                  ║
║  前端 (3 个文件)                                                  ║
╠══════════════════════════════════════════════════════════════════╣
║  🔗 API 接口                                                      ║
║  POST /api/Product/queryByPage                                   ║
║  GET  /api/Product/queryById                                     ║
║  POST /api/Product/insert                                        ║
║  PUT  /api/Product/update                                        ║
║  DELETE /api/Product/delete                                      ║
╚══════════════════════════════════════════════════════════════════╝
```
