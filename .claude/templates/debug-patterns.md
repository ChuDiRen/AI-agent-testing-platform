# 调试排查模板库

本文件包含调试流程和常见错误模式，供调试相关插件引用。

## 排查流程

```
┌─────────────────────────────────────────────────────────────────┐
│                      🔍 问题排查流程                             │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  1️⃣ 复现问题                                                    │
│     ├── 获取错误信息/堆栈                                        │
│     ├── 确认复现步骤                                             │
│     └── 记录环境信息                                             │
│                                                                 │
│  2️⃣ 定位范围                                                    │
│     ├── 前端 or 后端?                                           │
│     ├── 哪个模块/文件?                                          │
│     └── 最近修改?                                               │
│                                                                 │
│  3️⃣ 分析原因                                                    │
│     ├── 查看日志                                                │
│     ├── 断点调试                                                │
│     └── 对比正常流程                                             │
│                                                                 │
│  4️⃣ 验证修复                                                    │
│     ├── 修复代码                                                │
│     ├── 回归测试                                                │
│     └── 确认解决                                                │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 常见错误速查表

### 后端错误

| 错误信息 | 可能原因 | 排查方向 |
|----------|----------|----------|
| `Connection refused` | 服务未启动/端口错误 | 检查服务状态、端口配置 |
| `Table doesn't exist` | 表未创建/名称错误 | 检查迁移、表名大小写 |
| `IntegrityError` | 主键/外键/唯一约束冲突 | 检查数据关系、重复数据 |
| `AttributeError: 'NoneType'` | 查询结果为空 | 添加空值检查 |
| `TimeoutError` | 数据库/API 超时 | 检查慢查询、网络连接 |
| `ImportError` | 模块未安装/路径错误 | 检查依赖、PYTHONPATH |
| `JSONDecodeError` | JSON 格式错误 | 检查请求体格式 |
| `ValidationError` | Pydantic 校验失败 | 检查字段类型、必填项 |

### 前端错误

| 错误信息 | 可能原因 | 排查方向 |
|----------|----------|----------|
| `Cannot read property of undefined` | 对象未初始化 | 添加可选链 `?.` |
| `Network Error` | 跨域/服务不可达 | 检查 CORS、代理配置 |
| `404 Not Found` | 路由/API 路径错误 | 检查路由配置、API 地址 |
| `Maximum call stack exceeded` | 无限递归/循环引用 | 检查递归终止条件 |
| `Uncaught Promise rejection` | 未处理的异步错误 | 添加 try-catch |
| `Component is not defined` | 组件未注册/导入 | 检查 import 语句 |
| `Invalid prop type` | Props 类型不匹配 | 检查组件 props 定义 |

---

## 调试命令

### 后端调试
```bash
# 查看日志
tail -f logs/app.log | grep ERROR

# 数据库连接测试
python -c "from core.database import engine; print(engine.url)"

# API 测试
curl -X POST http://localhost:8000/api/test -H "Content-Type: application/json" -d '{}'

# 启动调试模式
uvicorn main:app --reload --log-level debug
```

### 前端调试
```bash
# 查看构建错误
npm run build 2>&1 | head -50

# 清理缓存
rm -rf node_modules/.vite
npm run dev

# 检查依赖
npm ls vue
```

### 数据库调试
```sql
-- 查看慢查询
SHOW PROCESSLIST;

-- 查看表结构
DESCRIBE table_name;

-- 查看索引
SHOW INDEX FROM table_name;
```

---

## 日志分析模板

```python
# 后端日志配置
import logging

logging.basicConfig(
    level=logging.DEBUG,
    format='%(asctime)s [%(levelname)s] %(name)s:%(lineno)d - %(message)s'
)

# 关键位置添加日志
logger.debug(f"Input params: {params}")
logger.info(f"Processing started for {item_id}")
logger.warning(f"Slow query detected: {query_time}ms")
logger.error(f"Operation failed: {error}", exc_info=True)
```

---

## 输出报告格式

```
╔══════════════════════════════════════════════════════════════════╗
║                    🔍 问题排查报告                                ║
╠══════════════════════════════════════════════════════════════════╣
║  📋 问题描述                                                      ║
║  ─────────────────────────────────────────────────────────────   ║
║  用户登录接口返回 500 错误                                        ║
╠══════════════════════════════════════════════════════════════════╣
║  🔎 排查过程                                                      ║
║  ─────────────────────────────────────────────────────────────   ║
║  1. 查看错误日志：IntegrityError at authService.py:45            ║
║  2. 定位代码：用户表主键冲突                                      ║
║  3. 分析原因：并发注册导致 ID 重复                                ║
╠══════════════════════════════════════════════════════════════════╣
║  ✅ 根因分析                                                      ║
║  ─────────────────────────────────────────────────────────────   ║
║  用户注册时手动生成 ID，高并发下产生重复                          ║
╠══════════════════════════════════════════════════════════════════╣
║  💡 修复方案                                                      ║
║  ─────────────────────────────────────────────────────────────   ║
║  1. 改用数据库自增 ID                                             ║
║  2. 添加唯一约束                                                  ║
║  3. 增加并发锁                                                    ║
╚══════════════════════════════════════════════════════════════════╝
```

---

## 性能问题排查

### 后端性能
```python
# 慢查询分析
import time

start = time.time()
result = slow_operation()
elapsed = time.time() - start
if elapsed > 1.0:
    logger.warning(f"Slow operation: {elapsed:.2f}s")
```

### 前端性能
```javascript
// 组件渲染分析
import { onMounted, onUpdated } from 'vue'

let renderCount = 0
onUpdated(() => {
  renderCount++
  console.log(`Component rendered ${renderCount} times`)
})
```

### 数据库性能
```sql
-- 开启慢查询日志
SET GLOBAL slow_query_log = 'ON';
SET GLOBAL long_query_time = 1;

-- 分析查询计划
EXPLAIN SELECT * FROM users WHERE name = 'test';
```
