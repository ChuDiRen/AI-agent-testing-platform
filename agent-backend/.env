# LangGraph 数据库配置（生产模式使用 DATABASE_URI）
# 注意：agent-backend_run.py 会自动设置 DATABASE_URI 指向 sqlite_storage/data/langgraph_server.db
DATABASE_URI=sqlite:///sqlite_storage/data/langgraph_server.db

# API Keys
DEEPSEEK_API_KEY=sk-f79fae69b11a4fce88e04805bd6314b7

# Redis 配置（使用 fake 模式，不需要真实 Redis）
REDIS_URI=fake

# 运行时版本（community 支持 SQLite 持久化）
LANGGRAPH_RUNTIME_EDITION=community